# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Build
dist/
build/
.env

# 작업용 문서 (커밋 제외) — 단, README가 참조하는 구조도는 포함한다
docs/*
!docs/images/
docs/images/*
!docs/images/architecture.html
!docs/images/architecture.png

# 작업용 시안 (커밋 제외)
mockups/

# images/ 는 통째로 작업용이다 — 코드가 여기를 읽는 곳은 한 군데도 없다.
#   화면에 나가는 보스 이미지: 관리자에서 등록할 때 S3로 올라가고 /api/images로 받는다
#   난이도 표시: 이미지가 아니라 글자+색(pc/admin/constants.js)
#   타이머 템플릿: frontend/src/features/timer/{icon,minimap,rune,booster}/ 가 원본
images/
